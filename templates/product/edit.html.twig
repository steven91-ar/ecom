{% extends 'base.html.twig' %}

{% block title %}Modification de l'article{% endblock %}

{% block body %}
<section class="product-edit-page">
    <h1>Modification de l'article</h1>
    <div class="action-links">
        {{ include('product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <a href="{{ path('app_product_index') }}" class="custom-link">Retourner à la liste</a>
        {{ include('product/_delete_form.html.twig') }}
    </div>
</section>
{% endblock %}